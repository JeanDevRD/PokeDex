<div class="row mb-3">
  <h1>{{#if editMode}}Editar tipo de pokemon{{else}}Crear tipo de pokemon{{/if}}</h1>
  <hr />
</div>

<div class="row d-flex justify-content-center">
  <div class="col-12 col-sm-7">

    {{#if error}}
      <div class="alert alert-danger"><i class="bi bi-exclamation-triangle"></i> {{error}}</div>
    {{/if}}

    <div class="card shadow-sm">
      <div class="card-body">
        <form method="post" action="/pokemonTypes/{{#if editMode}}edit/{{type.id}}{{else}}create{{/if}}">

          <div class="mb-3">
            <label for="name" class="form-label fw-semibold">Nombre del tipo</label>
            <input type="text" id="name" name="name" class="form-control"
              placeholder="Ej: Fuego"
              value="{{#if editMode}}{{pokemonType.name}}{{else}}{{formData.name}}{{/if}}" required />
          </div>

          <div class="d-flex gap-2 mt-4">
            <a href="/pokemonTypes/index" class="btn btn-secondary">
              <i class="bi bi-arrow-left"></i> Volver
            </a>
            <button type="submit" class="btn btn-{{#if editMode}}primary{{else}}success{{/if}}">
              <i class="bi bi-check-circle"></i>
              {{#if editMode}}Guardar cambios{{else}}Crear tipo{{/if}}
            </button>
          </div>

        </form>
      </div>
    </div>
  </div>
</div>